export const Header: React.FC = () => {
  return (
    <header className="mb-8 flex items-center justify-between">
      <div>
        <h1 className="text-3xl font-bold text-neutral-900">Habit Tracker</h1>
        <p className="text-neutral-500">Stay consistent, one day at a time.</p>
      </div>
      {/* We will add a button here later */}
    </header>
  );
};